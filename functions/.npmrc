# Cloud Functions のクラウドビルドは npm を使う。
# devDependencies (rules テスト用の firebase@11 と @firebase/rules-unit-testing@5 が
# 要求する firebase@12 の peer) が ERESOLVE で衝突するため、緩いピア解決にする。
# ランタイムは esbuild バンドル済みで dev 依存を必要としないので影響なし。
legacy-peer-deps=true
